---
apiVersion: v1
kind: Service
metadata:
  name     : db
  labels:
    app: db
spec:
  selector:
    app: db
  ports:
    - name      : app-listener
      protocol  : TCP
      port      : 34000
      targetPort: 3306
  type: ClusterIP
