apiVersion: v1
kind: Service
metadata:
  name: midjourney
  labels:
    app: midjourney
spec:
  type: ClusterIP
  selector:
    app: midjourney
  ports:
    - port: 34003
      targetPort: 9020
      protocol: TCP
