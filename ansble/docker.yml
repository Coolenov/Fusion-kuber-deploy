---
- name  : Configure docker
  hosts : all
  become: yes

  tasks:
    - name : Install Docker
      shell: apt install docker docker.io -y

    - name : Autostart Docker
      shell: systemctl enable docker

    - name : Restart Docker
      shell: systemctl restart docker